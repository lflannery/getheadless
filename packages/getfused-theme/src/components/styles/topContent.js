import { styled } from 'frontity'

const TopContent = styled.div`
  text-align: center;

  .featuredImage {
    margin: -2rem auto 0;
    display: block;
    max-width: 100%;
  }
`
export default TopContent;