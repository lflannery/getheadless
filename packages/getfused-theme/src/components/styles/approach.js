import { styled } from 'frontity'

const Approach = styled.div`
  margin: auto;
  max-width: 880px;
  padding: 2rem;

  a {
    font-size: 1.5rem;
    font-weight: 500;
    color: inherit;
  }
`
export default Approach;